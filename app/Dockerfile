FROM node:18.17.1
COPY . /home/app
WORKDIR /home/app

ARG LOGDIR
ARG LOGLEVEL
ARG DOCKER

ENV LOGDIR $LOGDIR
ENV LOGLEVEL $LOGLEVEL
ENV DOCKER $DOCKER

RUN npm ci

EXPOSE 8001

RUN mkdir -p $LOGDIR

CMD npm start


